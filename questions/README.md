# Questions

## What is a Go routine?
A lightweight thread of execution.

## Go `Compile Errors` to enforce better code quality

## Go is a statically typed Language
 - You need to tell Go Compiler , the data type when declaring the variable

## Type Inference
 - but Go can infer when you assign a value.

## Discover mistakes at compile time, NOT at runtime

